# Configuration pour l'environnement de test
APP_ENV=test
APP_SECRET=test_secret_key_for_testing_only
# Base de données SQLite en mémoire pour tests rapides et sans configuration
DATABASE_URL="sqlite:///:memory:"
# Alternative MySQL si disponible
# DATABASE_URL="mysql://root:@localhost/todolist_test?unix_socket=/Applications/XAMPP/xamppfiles/var/mysql/mysql.sock&serverVersion=8.0.32"
